package com.laptrinhjavaweb.api.web;

import org.springframework.web.bind.annotation.RestController;

@RestController(value = "monAPIOfWeb")
public class MonAPI {

	
	/*
	 * @Autowired private IMonService monService;
	 * 
	 * @PostMapping("/api/new") public MonDTO createMon(@RequestBody MonDTO monDTO)
	 * { return monService.save(monDTO); }
	 * 
	 * @PutMapping("/api/new") public MonDTO updateMon(@RequestBody MonDTO
	 * updateMon) { return monService.save(updateMon); }
	 * 
	 * @DeleteMapping("/api/new") public void deleteMon(@RequestBody long[] ids) {
	 * monService.delete(ids); }
	 */
}
